import { DynamicContainer as Div } from '@root/components/DynamicContainer'
import { ThemedText as Text } from '@root/components/ThemedText'

export default function SigninScreen() {
    return (
        <Div>
            <Text>signin</Text>
        </Div>
    )
}